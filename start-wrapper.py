#!/usr/bin/env python
from wrapper import Wrapper

# Start script for development purposes
# For non-development purposes, use `mcwrapper` 
# command after installation

if __name__ == "__main__":
    wrapper = Wrapper()
    wrapper.start()
